## 双向循环神经网络

双向 RNN 的动机：

> 有些任务判断当前位置时，不仅要看“前文”，还要看“后文”。

比如填空：

```text
我 ___ 。
我 ___ 饿了。
我 ___ 饿了，我可以吃半头猪。
```

同一个空，后面的上下文不同，答案也可能不同。

普通 RNN 只能从左往右利用过去信息；双向 RNN 会：

- 一条 RNN 从左往右读
- 一条 RNN 从右往左读
- 最后把两边的信息结合起来

所以它适合：

> 文本分类、序列标注、命名实体识别这类“整段文本都已经知道”的任务。

但不适合严格自回归生成，因为生成未来 token 时不能提前看到未来。

### 隐马尔可夫模型中的动态规划

