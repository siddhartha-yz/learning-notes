独热编码(one-hot encoding)
用一个”只有一个位置是1“，”其余位置都是0“的向量来表示类别。
数字本身会带来一些不存在的数学关系，故只用0和1

和softmax连起来理解：
假设:
$$
\text{猫}=(1,0,0)\qquad\text{鸡}=(0,1,0)\qquad\text{狗}=(0,0,1)
$$
假设神经网络最后输出
$$
\hat{y}=(0.8,0.15,0.05)
$$
可以理解为模型认为:

