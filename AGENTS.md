# 仓库级指导

## 学习实践题库原则

以下是本仓库持续适用的出题原则：**高质量、数量少、贴近机器学习／深度学习、交互性高**。

- 先阅读对应笔记，明确来源与知识边界。通常每篇笔记设计 2–3 道精心组织的任务，不为覆盖每个命令拆成大量机械题。
- 场景必须服务于 AI 专业学习中的实际工作，例如数据集划分、实验配置、训练日志、模型检查点。不能只替换文件名来包装与场景无关的练习。
- 让学生执行、观察、修改、验证并提交简短解释；一次任务应有明确目标、可见结果和成就感。操作应由桌面应用真实实现，不以选择题或纯文本模拟替代必要交互。
- 只要求当前及已学笔记的知识；新增概念必须在题目中说明。使用少量有意义的错误或干扰项，不设计隐蔽的陷阱。
- 题目必须说明初始环境、目标路径、要保留的内容、提交要求及必要限制。检查项和 Agent 评分要求不得暗中追加任务。
- 判题接受符合要求的等效命令。涉及文件操作时验证最终状态和内容；必要时结合执行证据，不能只匹配命令字符串或学生自述。
- 通过时明确标记并提供下一题入口，不追加拓展问题、无关思考或额外作业；未通过时只说明本题缺失项与必要修正。
- 保存题目快照、原始答题内容、命令输出和判题结果，方便复盘与改进。区分真实学生日志、历史摘要和自动测试记录，不伪造缺失历史，不把模拟模型结果说成真实 API 验证。
- 文件修改和删除必须发生在隔离临时环境中；不得让练习访问个人笔记或 API 密钥。重置能恢复初始环境。
- 新题必须实际走通正向完成路径和有意义的失败路径；同时验证旧题、切题、完成提示和日志保存，测试不得污染学生进度。

## 仓库协作

- 保留用户尚未提交的笔记改动，只暂存本次工作涉及的文件。
- 本项目的代码、题目、指导文档与用户授权保存的答题记录应提交并推送到 learning-notes 仓库。API 密钥和本机连接配置不进入 Git。
- 原有 `note` 命令继续用于上传学习笔记与答题记录。应用本身不在每次提交答案时自动推送。

## PyTorch 实践课程（用户已确认）

- 目标是通过代码理解模型：把公式、张量轴、数据流和梯度依赖对应起来，不能仅以背 API 或照抄示例为目标。
- 学习者 Python 基础尚可，张量、形状变换与自动求导从头练。按能力依赖组织 PyTorch 课程，不套用 Linux 的笔记日期章节。
- 当前先实现张量基础六题，分三小节；完整进阶路线见 `practice-lab/pytorch/ROADMAP.md`。自动求导及后续阶段根据真实作答迭代，不将计划中的功能说成已完成。
- 一道核心题可投入 20–40 分钟。先在默认可见的教学页学习概念、必要语法和逐行解释的小例子，实际运行、修改、观察，再预测并独立实现、用变形测试检验、作简短解释。PyTorch 语法按零基础处理，不要求自行查文档拼凑；基础 API 不藏在提示按钮里。主动提示用于额外解题帮助，教学例子使用不同数据帮助迁移，不直接填好题目函数。教学运行不得标记本题通过，并单独记录日志。
- 本地测试立即反馈，不调用模型；正式提交时本地结果与 Agent 对解释的评审均满足才通过。初次预测错误但最终能正确解释修正，不应因此拒绝通过。
- 不用样例输出恰巧正确掩盖形状、轴或权重语义错误；测试应包含有辨别力的非方形批次、单样本、不同有效序列长度等。所有额外输入条件应写在题目契约中。
- 通过次数不等于熟练度；不声称几题能保证“炉火纯青”。后续用独立重写、有意义变式与间隔复习观察迁移能力。
