# 26.9.6 题库验证

依据 `Linux系统学习/26.9.6.md`，本章精选三题：寻找大检查点、定位训练错误并计数、导出验证摘要。暂不包含 Vim 和持续跟踪任务，避免将未实现的交互设为过关条件。

实际使用隔离环境内的 which、find、grep、wc、echo、tail 与 Bash 管道、重定向运行，无需模型权重、GPU 或额外训练库。大文件是稀疏占位文件；大小阈值避开 GNU find 的取整边界，题目明确 M 的单位为 MiB。

正向验证：找出且仅找出两个大检查点；定位第 3、5 行错误并统计为 2；覆盖 STALE、保留两条验证指标、追加项目路径与状态，摘要内容完全匹配。

失败验证：按大小漏筛扩展名会混入 debug.log；整个日志计数为 6，不能当成错误数；管道上游文件不存在不能伪装成功；追加遗留内容或覆盖掉验证记录均失败；原始指标被修改会失败。额外验证管道每阶段命令限制、任意命令替换拒绝、数据目录与宿主机路径不可写。

测试期间发现本机 which 经由 /etc/alternatives 链接，已为隔离环境单独映射它实际指向的程序，不挂载整个宿主机 /etc。

32 项自动测试通过，GTK 验证走通 26.9.4 / 26.9.6 六道题的真实操作、模拟评审、日志、按章导航、完成提示，以及旧题回归。测试使用临时进度和日志，不污染学生记录；API 返回使用模拟结果，不代表真实模型评审验收。

复现：

```bash
python3 -m unittest discover -s practice-lab -v
python3 practice-lab/ui_smoke.py
```
