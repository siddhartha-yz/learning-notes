# PyTorch 零基础教学验证（2026-09-09）

问题：原有第一阶段只有任务、函数骨架与主动提示，缺少零基础语法教学。现在六题默认打开教学页，包含概念、必要语法、逐行例子与修改观察；可切换独立练习并返回教学。

- 完整回归 `python3 -m unittest discover -s practice-lab -p 'test_*.py'`：39 项通过（145.883 秒）；另补测 softmax 修改后的列和为 1、行和不为 1。
- `test_tutorials.py`：真实执行六个原始例子与六个修改实验，验证单样本形状、错误展平、错误统计轴、softmax 轴变化、成对索引及 padding 不变性；另测语法错误、SystemExit 和输出截断。
- `pytorch_ui_smoke.py`：教学默认可见，无预测运行、教学代码草稿恢复、例子重置、独立日志与不计通过；六道真实实现回归、模拟评审失败重试、代码变化后重新测试、切换课程，共 14 条临时日志。
- `ui_smoke.py`：Linux 章节、六条真实操作流程、分页、完成提示与日志回归通过。
- 已截图检查 GTK 默认教学页、代码编辑区、运行结果区与进入练习按钮；讲解与观察区保留可见滚动条。

测试使用临时状态和日志，评审 API 使用模拟响应；没有调用用户模型 API，没有改变学生草稿或进度。
